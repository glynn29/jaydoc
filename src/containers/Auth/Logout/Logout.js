import React, {useEffect} from "react";

const Logout = props =>{

    return (<div>
        <p>Logout page</p>
    </div>);
    // const {onLogout} = props;
    //
    // useEffect(()=>{
    //     onLogout();
    // }, [onLogout]);
    // return <Redirect to="/"/>;
};

export default Logout;
